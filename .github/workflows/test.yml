name: SPOT Pre-Production Tests

on:
    pull_request:
        branches:
            - main
    workflow_dispatch:

jobs:
    backend-test:
        runs-on: ubuntu-latest
        steps:
            - name: Code Clone
              uses: actions/checkout@v4

            - name: Java 21 Setup
              uses: actions/setup-java@v4
              with:
                java-version: '21'
                distribution: 'temurin'
                cache: 'maven'

            - name: Spring Boot Testing
              run: |
                cd backend
                chmod +x mvnw
                ./mvnw test

            - name: Run Backend Integration Tests
              run: |
                cd backend
                chmod +x mvnw
                ./mvnw "-Dtest=UserIntegrationTest,SessionIntegrationTest,AttendanceIntegrationTest" test

            - name: Build Backend Docker Image
              uses: docker/build-push-action@v5
              with:
                context: ./backend
                push: false
                tags: spot-backend:test

    frontend-test:
        runs-on: ubuntu-latest
        steps:
            - name: Code Clone
              uses: actions/checkout@v4

            - name: Node.js Installation
              uses: actions/setup-node@v4
              with:
                node-version: '20'
                cache: 'npm'
                cache-dependency-path: './frontend/package-lock.json'

            - name: Dependencies Installation
              run: |
                cd frontend
                npm ci

            - name: Lint Frontend Code
              run: |
                cd frontend
                npm run lint

            - name: Run Unit Tests
              run: |
                cd frontend
                npm run test

            - name: Test Frontend Build (without Docker)
              run: |
                cd frontend
                npm run build

            - name: Install Playwright Browsers
              run: |
                cd frontend
                npx playwright install --with-deps chromium

            - name: Run Frontend E2E Tests
              run: |
                cd frontend
                npm run test:e2e -- tests/core-journey.spec.ts --project=chromium

            - name: Build Frontend Docker Image
              uses: docker/build-push-action@v5
              with:
                context: ./frontend
                push: false
                tags: spot-frontend:test