name: SPOT CI/CD Pipeline

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
    workflow_dispatch:

jobs:
    backend-build:
        runs-on: ubuntu-latest
        steps:
            - name: Code Clone
              uses: actions/checkout@v4

            - name: GitHub Container Registry Login
              uses: docker/login-action@v3
              with:
                registry: ghcr.io
                username: ${{ github.actor }}
                password: ${{ secrets.GITHUB_TOKEN }}

            - name: Build and Push Backend Docker Image
              uses: docker/build-push-action@v5
              with:
                context: .
                file: ./backend
                push: true
                tags: ghcr.io/${{ github.repository }}/spot-backend:latest

    frontend-build:
        runs-on: ubuntu-latest
        steps:
            - name: Code Clone
              uses: actions/checkout@v4

            - name: GitHub Container Registry Login
              uses: docker/login-action@v3
              with:
                registry: ghcr.io
                username: ${{ github.actor }}
                password: ${{ secrets.GITHUB_TOKEN }}

            - name: Build and Push Frontend Docker Image
              uses: docker/build-push-action@v5
              with:
                context: .
                file: ./frontend
                push: true
                tags: ghcr.io/${{ github.repository }}/spot-frontend:latest

    deploy:
        runs-on: self-hosted
        needs: [backend-build, frontend-build]
        steps:
            - name: Deploy to Server
              run: |
                cd /root/baam2.0
                sudo docker compose pull
                sudo docker compose up -d
